public class ArmouredEnemy extends Enemy {
    public ArmouredEnemy(int health) {
        super(health);
    }

    @Override
    public void hit(int damage) {
        super.hit(damage);
    }
}
